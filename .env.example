# Copy to .env before running locally.

# SQLite database location (relative to the prisma/ directory).
DATABASE_URL="file:./data/seerr2.db"

# Optional: seed the TMDB API key at boot. You can also set it in Settings after launch.
# Get a free key at https://www.themoviedb.org/settings/api
TMDB_API_KEY=""
